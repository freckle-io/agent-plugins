# AI Ark People

Read `freckle list build ai-ark people --help` for command syntax, inline filters, defaults, and preview output. Preview and approval rules live in [List building](../LIST.md#approved-preview-or-immediate-creation). Load this reference for advanced JSON filters.

## Advanced filters

Use `--input-json '<json>'` or `--search-file <path>` for the complete product filter object with optional `account`, `contact`, `advancedAccountFilters`, and `advancedContactFilters`. Preserve the approved nested object exactly.
